# 📚 Biblioteca Integral FICA – Frontend

Biblioteca Integral FICA is a web-based platform designed to allow students to rent university books online in an easy, fast, and secure way. This repository contains the **frontend** of the system, which provides the user interface for interacting with the library services.

The frontend is responsible for displaying available books, managing user interactions, and communicating with the backend services to handle rentals and user data.

## 🚀 Project Description

The frontend application is built with **React** and focuses on delivering a clean and responsive user experience for university students. It allows users to browse academic books, view details, and interact with the system through a modern web interface.

## 🛠 Technologies Used

- **React** – User interface development
- **Node.js** – JavaScript runtime for development
- **Docker** – Containerization for consistent deployment
- **Nginx** – Web server for production environments

## 📁 Project Structure

```
FrontEnd/
├── public/              # Static files and main HTML template
├── src/                 # React components, pages, and logic
├── .dockerignore        # Files excluded from Docker images
├── .gitignore           # Git ignore rules
├── Dockerfile           # Docker configuration for the frontend
├── nginx.conf           # Nginx configuration file
├── package.json         # Project dependencies and scripts
├── package-lock.json    # Dependency lock file
└── README.md            # Project documentation
```

## 🎯 Purpose

This frontend serves as the client-side application of the Biblioteca Integral FICA system. It is designed to be intuitive, responsive, and easily maintainable, ensuring a smooth experience for students accessing university library resources online.
